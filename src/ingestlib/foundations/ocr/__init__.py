"""OCR primitives — layout detection + recognition of text, tables, formulas, charts, seals.

Public sub-modules:
    models    — BoundingBox, Region, LayoutResult, RegionType
    paddle_vl — PaddleOCR-VL wrapper (layout + VLM recognition — the parse engine)
"""
